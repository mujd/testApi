{% extends 'layoutWindow.html.twig' %}

{% block modal %}
    {{ form_start(form) }}
    <div class="form-group">
    {{ form_label(form.nombre) }}
    {{ form_widget(form.nombre, {'attr' : {'placeholder' : 'Nombre', 'class': 'form-control'}}) }}
    <span >{{ form_errors(form.nombre) }}</span>
    </div>
    <div class="form-group">
    {{ form_label(form.definicion) }}
    {{ form_widget(form.definicion, {'attr' : {'placeholder' : 'Definición', 'class': 'form-control'}}) }}
    <span >{{ form_errors(form.definicion) }}</span>
    </div>  
    {#<div class="form-group">
        {{ form_label(form.indicador) }}
        {{ form_widget(form.indicador, {'attr' : {'class': 'form-control'}}) }}
        <span >{{ form_errors(form.indicador) }}</span>
    </div>#}
    <br>
    <div class="row">
        <div class="col-md-12 text-right">  
            {{ form_widget(form.save, {'label' : 'Guardar', 'attr' : {  'class': 'form-control', 'class': 'btn btn-info'}}) }}
            <button type="button" class="btn btn-sm btn-default" data-dismiss="modal" onclick="parent.cerrarCompetenciaConductual()">Cerrar</button>
        </div>
    </div>
    {{ form_end(form) }}

{% endblock %}